# STATS

```r
# Linear Model

lm( x ~ y)
lm (x ~ poly(x,2))
plot(x, y)

abline(lm(y~x))
```

```r
x <- c(1,2,3,4)

# Generate Sequence

seq(from =  a, to = b, by = c
```

```r
x = 10
if (x>0) 
{
print("x")
}
else
{
print("Odd No.")
}

r < - c(6,5,8,3)
for(i in r)
{
print(i)
}

while(i < 10)
{
i
}
```

```r
matrix(data = a, nrow = 3, ncol = 4, byrow = "")

d1 = data.frame(x,y)

plot(1, 3) is a point

plot(c(1,8), c(3,10) is 2 points (1,3) and (8,10)

barplot(H, xlab, ylab, main, names.org, col) 

col is color, main is title, 
names.arg are the names under each bar

```

```r
pie(x, labels, radius, main, col, clockwise)

x -> data
labels - > names of partitions
main - > title of pie
radius - > size of chart
col - > color

```

```r

```